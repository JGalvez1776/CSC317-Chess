<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Classic Theme (default) -->
    <style name="Theme.Classic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Theme colors -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorTertiary">@color/yellow</item>
        <item name="colorAccent">@color/gray</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
    </style>

    <!-- Wooden Theme -->
    <style name="Theme.Wooden" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Theme colors -->
        <item name="colorPrimary">@color/dark_wood</item>
        <item name="colorSecondary">@color/light_wood</item>
        <item name="colorTertiary">@color/yellow</item>
        <item name="colorAccent">@color/dark_wood</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
    </style>

    <!-- Olive Theme -->
    <style name="Theme.Olive" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Theme colors -->
        <item name="colorPrimary">@color/dark_olive</item>
        <item name="colorSecondary">@color/light_olive</item>
        <item name="colorTertiary">@color/yellow</item>
        <item name="colorAccent">@color/dark_olive</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
    </style>

    <!-- Night Theme -->
    <style name="Theme.Night" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Theme colors -->
        <item name="colorPrimary">@color/dark_night</item>
        <item name="colorSecondary">@color/light_night</item>
        <item name="colorTertiary">@color/yellow</item>
        <item name="colorAccent">@color/dark_night</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
    </style>
</resources>